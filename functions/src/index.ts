/**
 * Firebase Cloud Functions entry point for SpeedType.
 *
 * Exports all callable and trigger-based functions so the Firebase
 * CLI can discover and deploy them.
 */

export {validateScore} from "./validateScore";
